# CarBox — project instructions

CarBox is a mobile-first car enthusiast app: build/maintenance logbook + goal-based mod recommendations + shareable garage page. This folder contains the approved design and the start of the frontend.

## What's here
- `app/index.html` — the **Garage screen, APPROVED by the owner**. This is the golden reference. Its design tokens, spacing, fonts, shadows, and asset usage define the entire app. Never restyle it.
- `app/assets/` — icons and the pixel car sprite, **cropped pixel-for-pixel from the design mockups with transparent backgrounds**. These are sacred: NEVER regenerate, redraw, resize-and-save, or substitute them with icon libraries (no lucide, no font-awesome). Use them via `<img>` exactly as index.html does.
- `*.png` in the root — the design mockups (source of truth for layout):
  - `Untitled design (26).png` — Garage screen (built ✓)
  - `Untitled design (27).png` — Log screen (timeline)
  - `hf_...152254....png` — Upgrades screen (goal + stages + shops map)
  - `hf_...153400....png` — Add Entry bottom sheet
  - `hf_...153622....png` — CarBox Pro paywall popup
  - `hf_...154101....png` — Entry detail screen
  - `hf_...154322....png` — Goal picker screen

## Design system (sampled from mockups — do not invent new values)
- Brown accent `#615142`, soft brown `#7F7269`, active-tab pill `#C9C2BA`
- ALL pages share the Garage background: `#F4F4F4` with a `#F9F9F9` band behind the nav (the shared `.phone` style in `app/style.css`). Owner decision — do NOT use the mockups' warm cream backgrounds.
- Card surface `#F7F7F7`, white pill `#FDFDFC`, hairline `rgba(0,0,0,.10)`
- Shadow `0 10px 26px rgba(0,0,0,.07)`; cards radius 18–22px; pills fully rounded
- Fonts: 'DM Serif Display' for display titles/nav labels ONLY; 'League Spartan' (400–700) for everything else (Google Fonts). Owner decision 2026-07-20: League Spartan REPLACED Fredoka everywhere — do not reintroduce Fredoka.
- Bottom nav (owner decision 2026-07-20): 270×56px pill bar with a sliding `.navpill` highlight (`#C9C2BA`), spring-animated and draggable via the shared `app/nav.js`; each page sets `data-active` on `.nav`. Page scrollbars are hidden (scrolling still works).
- DARK PALETTE (approved 2026-07-21, sampled from the three dark reference renders — all values live ONLY under `html[data-theme="dark"]` in style.css): page/bg `#1D1C1C`, bottom band `#211F20`, card/surface `#292827`, pills+nav `#211F1F`, sheet `#252322`, control track `#3D3B39`, input `#312E2D`. Warm clay accent `#9D8573` (reminder/goal/FAB/see-all/MOD+STAGE chips), rail+dots `#9B8372`, active nav pill `#958274` (label stays `#2A2521`), accent text `#B49C86`. Text: primary `#F2EEE9`, secondary `#D9D3CC`, muted `#A29A93`, meta `#A49B94`. Chips: MAINT `#8A8683`/text `#2E2B29`, REPAIR `#646467`/text `#EDECEF`. Stat circles `#565350`, badge `#D9D3CD`/text `#2A2521`, avatar ring `#A08B77`. Map uses baked `assets/map_shops_dark.png` (dark ground, grey streets, brown pins) via `content:` swap. Icon pattern: `*_dark.png` cream variants or filters — never plain brightness() on antialiased icons.
- Phone shell: max-width 375px centered; fixed bottom nav (copy the nav from index.html verbatim onto every tab screen)

## Working rules
1. Build ONE screen at a time. Match the corresponding mockup PNG closely; where the mockup and index.html conventions conflict, follow index.html.
2. New image assets (map pins, goal icons, camera icon, paywall icons, photos) must be **cropped out of the mockup PNGs** with Python/PIL: crop generously → flood-fill the background to transparent (tolerance ~30 against the corner background color) → trim to the alpha bbox → size in CSS at cropped-pixels ÷ 3 (for 1125-wide mockups) or ÷ 4.1 (for 1536-wide mockups). Visually verify every crop before using it.
3. Sizes: mockup pixels ÷ 3 = CSS px (1125-wide mockups). Measure, don't guess.
4. Keep it vanilla HTML/CSS/JS multi-page (one file per screen, shared `style.css` extracted from index.html) until ALL screens are visually approved. Only then, if asked, port to React.
5. State: vanilla JS + localStorage (entries, likes, goal, plan items, isPro). Seed data must match the mockups (Bugatti Chiron, 82,410 mi, the 4 log entries, $48,200 total).
6. After each screen, stop and ask the owner to review in the browser before continuing.
7. The app name is **CarBox**. Bottom nav labels: `Garage`, `log`, `upgrades` (exact casing).

## Screen order
1. Log (timeline) → 2. Upgrades → 3. Goal picker → 4. Entry detail → 5. Add Entry bottom sheet (overlay on Log) → 6. Pro paywall popup → 7. Wire navigation + localStorage state → 8. Settings (no mockup — reuse existing components/patterns only)

## Status (2026-07-20)
- Built + interactive: Garage, Log, Upgrades, Goal picker, Entry detail, Settings, Onboarding. Shared infra: `app/state.js` (store, localStorage "carbox.v1"), `app/ui.js` (toast/sheet/pressable/stagger/count-up), `app/nav.js` (draggable spring nav pill).
- Specs DB (2026-07-22): `app/data/specs/<brand>.json` — 14 brands / 471 verified model configurations (Audi, BMW, Ford, Honda, Hyundai, Jeep, Kia, Lexus, Mazda, Mercedes-Benz, Nissan, Subaru, Tesla, Toyota), US-market 2010+, web-verified against 0-60specs.com. Schema + integrity rules in `SCHEMA.md`; `index.json` has per-brand counts; `BUILD_LOG.md` has the process notes. 36 honest `null`s (unverifiable figures, never invented). Coverage gaps (missing models/trims per brand) are noted in each file's `note`. Regenerate the app manifest after edits: `scratchpad/gen_cars.py` → `app/data/cars.js`.
- Car manifest (2026-07-22): `app/data/cars.js` — AUTO-GENERATED from the specs DB, loaded as a `<script>` (no fetch; works on file:// + Expo). Exposes `window.CarBoxCars`: `.brands()`, `.models(brand)`, `.lookup(brand,model,year)` → best-matching generation's core-6 specs (`{engine,horsepower,torque,transmission,drivetrain,accel}`, plus `trim`/`generation`/`yearStart`/`yearEnd`/`exact`). Lookup picks the generation whose year range contains the year, else the nearest.
- Onboarding car step reworked (2026-07-22): make + model are now `<select>` dropdowns sourced ONLY from the specs DB (`CarBoxCars`); year enforces the 2010+ policy (`MIN_YEAR` in onboarding.js — pre-2010 is declined inline). Same step now hosts the full appearance customizer (7 presets + hue slider + 8 monotone swatches + Original-grey), identical behavior to Settings' My Car; choices held in the in-memory `A` object and committed at step 6 as `car:{presetId,hue}`. On finish, `vehicle.specs` is filled from `CarBoxCars.lookup` for the chosen make/model/year, and `vehicle.trim` is stored. Finishing onboarding also resets the personal activity to a FRESH account (owner decision 2026-07-22): `entries`/`planItems`/`comments` cleared, `likes`/`stats` zeroed, one welcome `notifications` item, `nextService` → First service @ 5,000 mi — so the whole app reflects the new car, not the Bugatti seeds. Theme/units/currency prefs are kept. (The DEFAULTS seeds in state.js are untouched — they only ever show pre-onboarding, which is gated.) Settings → Account has: working **Sign out** (clears `onboardingComplete` → onboarding, data kept), **Delete account** (wipes `carbox.v1`), and a TEMP dev row **Redo onboarding** (`.devrow`, re-runs signup); the first two use a shared `confirmSheet` helper.
- Garage specs are now LIVE (2026-07-22): the six spec rows render from `vehicle.specs` (`paintSpecs` in index.html), and a small clay **Edit** pill above the list opens a sheet (six prefilled fields) to hand-edit them. `null`/empty specs render as "—". `vehicle.specs` shape changed 5→6 fields (added `torque`,`transmission`; dropped `tire`); the dead top-level `specs` array was removed from state.js. (Garage pixel sprite still uses the chosen preset, not a per-model render — unchanged limitation.)
- Onboarding (added 2026-07-21): `app/onboarding.html` + `app/onboarding.js` — 7-step signup (Welcome → Account → Username&tag → Birthday → Car → Terms&Privacy → Done), JS step machine, answers held in memory and committed to the store only at step 6. `MIN_AGE` constant (=16, CarBox policy, enforced) at top of onboarding.js; password is stored plaintext locally for UI only (NOT secure — needs real backend auth, flagged in-file). Curated `CARS` dataset auto-fills `vehicle.specs`. New state fields: `onboardingComplete` (bool), `account` {firstName,lastName,email,password}, `birthday`, and `vehicle` now carries `{name,make,model,year,mileage,specs{engine,horsepower,drivetrain,accel,tire}}`. Gate: the pre-paint `<script>` in index/log/upgrades redirects to onboarding.html unless `onboardingComplete`; `CarBox.requireOnboarding()` mirrors it. NOTE: Garage's pixel sprite + 6 hardcoded spec rows still show demo data until a later feature wires `vehicle.specs`/sprite to the entered car — only name/year/tag hydrate live today. Expo shell `DARK` bumped to `#1D1C1C` to match the dark page bg (no more darker safe-area bezels).
- Upgrades is LIVE (2026-07-22): the goal pill is now a plain label (only **Change goal** opens the picker). The two stage cards were replaced by **2 AI mod recommendations** for the actual `vehicle` + `goal` (no stage labels, real mod names, benefit line, tap → detail sheet whose paragraph is dash-free — enforced in the system prompt AND stripped client/server-side). Recs come from a **backend proxy** in `/server` (keys server-side only): `POST /api/recommend` {make,model,year,trim,specs,goal} → 2 recs via Anthropic; `POST /api/shops` {lat,lng,modName} → live Google Places shops (name, haversine distanceMiles, live rating(+count), LLM/heuristic priceEstimate, mapsUrl); `GET /api/health`. Zero-dep Node runner `server/server.js` (port 8787), Vercel-ready `server/api/*.js`, env vars + deploy in `server/README.md` (ANTHROPIC_API_KEY, GOOGLE_MAPS_API_KEY). Client auto-targets `http://<page-host>:8787`, override with `localStorage['carbox.apiBase']`. If the proxy is down/keyless the page uses a clearly-commented **rules-based local fallback** (goal × aspiration × EV-aware) so it never breaks; recs are cached in state as `recs:{key:'make|model|year|trim|goal',items,source}` (only `source:'api'` is treated as fresh). **Add to plan / plan-total are gone** — each card has **Add to map** (state `mappedMod`); the mapped mod drives the shops section (one at a time, inset-brown selected state). Shops section states: location NOT granted → opaque `--brown-warm` block + `badge_lock.png` + explanation + re-request button; granted → live shops (no caching), skeleton/loading, error+retry, empty. **Directions** deep-links to Google Maps (`mapsUrl`), no more toast.
- Onboarding location step (2026-07-22): new **Step 6 of 8** (Car → Location → Terms → Done; progress /7, head hidden on 1&8) asks for geolocation with real why-copy (nearby shops, live distances/ratings/prices). Grant → `locationGranted:true` + `location:{lat,lng}`; decline/skip NEVER blocks signup (re-request lives in Upgrades' locked map). New state fields: `locationGranted`, `location`, `recs`, `mappedMod`. **Expo shell**: WebView now has `geolocationEnabled` (Android bridge) and app.json got iOS `NSLocationWhenInUseUsageDescription` + Android location permissions — rebuild the dev client for these to take effect; Expo Go's own location permission must be granted on the phone.
- Pro paywall is LIVE (`app/pro.js`, 2026-07-21): centered modal per mockup hf_...153622, opened from locked goals / settings Upgrade; "Start free week" flips `isPro` (no real payments) and unlocks goals via the `carbox-pro` event. Add Entry sheet is LIVE: FAB morphs into it, FLIP-inserts entries, photos preview, title required.
- Motion (2026-07-21): `UI.spring` physics for nav-pill/sheet gestures, directional View Transitions (`carbox.navdir`), car sprite morphs across pages (`carbox-car`), condensing headers via `UI.condense`, micro-interactions (like burst, toggle squash, bell swing, checkmark draw, pin drop). All respect prefers-reduced-motion.
- Motion rules: one easing family `cubic-bezier(.3,1.4,.4,1)`, fades ease-out, 180–450ms, transform/opacity only, `prefers-reduced-motion` → fades.
- Car presets reworked (2026-07-22, owner-supplied): the picker is exactly **5** body styles — `preset_suv`, `preset_suvcoupe`, `preset_coupe2` (2-door), `preset_coupe4` (4-door), `preset_sedan` — replacing the old 7. Source art (`Untitled design (27–31).png`, root) is painted GREEN on every color-changing pixel; `scratchpad/gen_presets.py` turns each into `assets/<id>.png` (green→greyscale so the DEFAULT car is black/white **with shadows**, all non-green pixels — glass, wheels, lights, outlines — kept verbatim) + `assets/mask_<id>.png` (opaque ONLY where green). So the mask, not a hand-drawn file, is derived from the green. To change what recolors, edit the green in the source and rerun the script. The **"Original grey" chip was removed** (default is now the greyscale base, `hue:null`); users recolor via the hue slider + 8 monotone swatches + a **DARKNESS row** (`car.shade` multiplier, `SHADES=[1,.82,.64,.47,.32]`, applied to masked body pixels only — see `UI.spritePainter.paint(canvas,hue,shade)` / `tintSprite(src,hue,shade,cb)`). Assets are named **`body_*`** (`body_suv`/`body_suvcoupe`/`body_coupe2`/`body_coupe4`/`body_sedan`) — renamed from `preset_*` specifically so the Expo WebView can't serve stale cached copies. Green detection is `(g-b)>=3 && g>=r-8` plus a morphological **close** (MaxFilter×2 → MinFilter×2) that fills tiny specular holes so light/white cars show no white dots. White/light monos use contrast around the tone (`v=M+(l-0.42)*1.15`) for deep shadows. Onboarding + Settings default preset is `body_suv`. (`sprite_chiron.png` stays only as the onboarding welcome/demo sprite, out of the picker.)
- Settings (rebuilt, no nav): collapsible My Car customizer — body-style presets (`assets/preset_*.png`) + hue tint via `UI.tintSprite` (canvas `color` composite; `{presetId, hue}` in state; every car render goes through `UI.carSprite`). Theme system: Light/Dark/System, `html[data-theme="dark"]` var overrides in style.css; light mode uses per-use `var(--x, <original hex>)` fallbacks and MUST stay pixel-identical; every page head has the pre-paint theme snippet. Currency (USD/EUR/GBP symbol-only) via `CarBox.fmtMoney`.
- Multi-car + real-functionality pass (2026-07-22): **state is now PER-CAR.** `state.cars[]` each hold `{id, vehicle, appearance, entries, planItems, goal, goalLocked, nextService, stats, likes, liked, comments}`; `state.activeCarId` names the shown one. `get/set` on the legacy per-car keys (`vehicle`, `car`→appearance, `entries`, `planItems`, `goal`, `goalLocked`, `nextService`, `stats`, `likes`, `liked`, `comments`) TRANSPARENTLY route to the active car, so page code was mostly unchanged. `load()` **migrates** a legacy flat single-car store into `cars[0]`. New API: `CarBox.cars()`, `activeCar()`, `activeCarId()`, `setActiveCar(id)`, `addCar(carObj)`, `removeCar(id)`, `maxCars()` (3 if Pro else 1). Top-level still: profile/account/theme/currency/isPro/units/reminders/notifsOn/recs/mappedMod/location. Notifications stay one top-level list; service items carry `carId`.
- Multi-car UI (Part B): Garage `.iconstack` shows a round tinted car button per car + a `+`, **only when Pro** (or when a lapsed user still holds extra cars). Active car has a ring; tapping switches active + re-hydrates. `+` → Add Car sheet (Pro under cap) / Pro paywall (free/lapsed) / "reached 3 cars" toast (Pro at 3). **Add Car** reuses the shared **`app/carform.js`** (`CarForm.mount(el,{onChange})` → make/model dropdowns from `CarBoxCars`, 2010+ year, 5 presets + hue + mono + darkness; `.cf-*` styles in style.css). **Pro lapse (B3):** extra cars are NEVER deleted; `setActiveCar` + `load()` restrict a non-Pro user to `cars[0]`, the rest show LOCKED with a resubscribe tap; restoring Pro unlocks them.
- Service intervals (Part C): `CarBox.computeServiceInterval(vehicle)` + `computeNextService(car)` in state.js (ONE tunable place; sensible approximations, not OEM-exact). Gas default 7,500 mi; 5,000 for turbo/supercharged/≥400 hp/pre-2015; EV → "Tire rotation / brake inspection" @ 7,500. Anchors to the highest-mileage `maint` entry (else current odometer). `set('entries'|'vehicle')` auto-recomputes `nextService`. Surfaced in the Garage pill, the notifications sheet, and the Log reminder as "due in X" / "overdue by X", all gated by the Settings **Service reminders** toggle; the Log reminder refreshes live when a maintenance entry is logged.
- Goal picker (owner change 2026-07-22): all 4 goals are now selectable (locks removed). Free users get ONE goal choice: Continue shows a confirm popup ("you can pick one goal, this will be it; changing later needs Pro") then sets `goalLocked` per car. A locked free user who tries to switch gets the Pro paywall. Pro users change freely.
- Garage share is REAL (Part D): the share button copies a link to **`app/garage.html?car=<id>`**, a read-only public view (sprite, name, year, 6 specs, build/maintenance timeline, likes) rendered from that car's data. Works on-device now; cross-device viewing needs the backend (see below). Likes/comments stay local but are commented as backend-swap points.
- Log photos + entry detail fixed (2026-07-22): Add Entry now persists up to **3** photos per entry as **compressed JPEG data URLs** (`compressPhoto`, max 1000px, q0.72) on `entry.photos`; they render as thumbnails on the timeline card (`.cardthumbs`) and in the entry-detail grid (`entry.html` renders `entry.photos` only — `.photos.n1/.n2/.n3`, the 3-photo layout = 1 big + 2 small; NO more hardcoded stock images). Demo entry `e1` carries the 3 `photo_exhaust_*.jpg` as its own photos. Logging the first entry now removes the "No entries yet" state and restores the rail. Hitting the 3-photo cap now **toasts** "You can add up to 3 photos" instead of silently stopping. **Entry detail Edit works** (`entry.html`): a sheet edits type/title/cost/mileage/date/notes/part/shop and adds/deletes photos (up to 3, same compressor), re-renders the detail on save, and has a Delete entry action. (For real cross-device storage, upload `pickedFiles` to cloud instead — same `entry.photos` shape.)
- Settings TEMP dev rows (Account): **Switch to Pro / Switch to Base** toggles `isPro` (fires `carbox-pro`) so Pro-gated features can be tested, alongside **Redo onboarding**. Remove all `.devrow` items before ship.

## Not yet real (needs backend / native)
These are stubbed with clean integration points, NOT faked:
- **Accounts / auth / sync** — no server; all data is per-device `localStorage` (`carbox.v1`). Onboarding password is UI-only, never secure.
- **Photo storage** — photos persist LOCALLY as compressed data URLs (max 3/entry) so they show now; real cloud object storage is still needed for cross-device and to avoid localStorage growth. Upload point marked in `log.html` (`pickedFiles` → `entry.photos`).
- **Public garage pages for other people** — `garage.html` reads the car from THIS device today; real sharing needs hosting + a DB the link can fetch by car id.
- **Real likes / comments / social** — local per-car today; needs DB + auth. Swap points commented in `index.html`.
- **Payments** — the Pro paywall only flips a local `isPro` flag. Real iOS subscriptions need StoreKit / In-App Purchase, and "Restore purchases" must call StoreKit restore.
- **Multi-device sync** — nothing syncs across devices without the backend.
- **AI recs / live shops** — already proxied through `/server` (keys server-side); needs `ANTHROPIC_API_KEY` + `GOOGLE_MAPS_API_KEY` set, else the app uses its local fallback / locked-map states.
